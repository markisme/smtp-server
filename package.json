{
  "name":"@types/smtp-server",
  "version":"1.0.1",
  "description": "TypeScript Definition for smtp-server",
  "scripts": {
    "test": "echo running server tests... && tsc ./test/test.ts"
  },
  "dependencies": {
    "typed-stream": "^1.0.1",
    "smtp-server": "1.15.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markisme/smtp-server.git"
  },
  "devDependencies": {
    "smtp-server": "1.15.0"
  }
}
